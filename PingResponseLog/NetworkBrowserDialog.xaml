<controls:MetroWindow x:Class="PingResponseLog.NetworkBrowserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        GlowBrush="{DynamicResource AccentColorBrush}" ShowIconOnTitleBar="False"
        Title="Browse Network" Height="300" Width="300" ShowMaxRestoreButton="False" ShowMinButton="False">
    <Grid >
        <ListBox Name="AddressListBox" Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding AddToAddresses}" Content="{Binding Name}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</controls:MetroWindow>

